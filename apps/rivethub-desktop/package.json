{
  "name": "@rivetos/rivethub-desktop",
  "version": "0.4.0-beta.6",
  "private": true,
  "description": "RivetHub desktop — thin Tauri shell over rivethub-web: tray, native escalation notifications, global shortcut",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  },
  "nx": {
    "tags": [
      "scope:app",
      "type:app",
      "domain:interfaces"
    ],
    "targets": {
      "build": {
        "dependsOn": ["@rivetos/rivethub-web:build"]
      }
    }
  }
}
