{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "RivetHub windows (main + additional win-*): native notifications; shortcuts are registered Rust-side. The shell never navigates to a remote origin — the bundled UI always stays put and only the gateway baseUrl is repointed (https gateways are dialed through the shell's loopback mTLS pipe) — so no remote URL grant is needed and none is given.",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "notification:default",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text",
    "opener:default"
  ]
}
