{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "RivetHub windows (main + additional win-*): native notifications; shortcuts are registered Rust-side",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
