{
  "name": "infra",
  "sourceRoot": "apps/infra/src",
  "projectType": "library",
  "tags": ["scope:tooling", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/infra/tsconfig.json"
      },
      "inputs": ["default", "^default"],
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/infra/tsconfig.json"
      },
      "inputs": ["default", "^default"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint apps/infra/src/"
      }
    }
  }
}
