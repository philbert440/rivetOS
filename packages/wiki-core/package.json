{
  "name": "@rivetos/wiki-core",
  "version": "0.4.0-beta.6",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/ package.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "vitest": "^4.1.10"
  },
  "nx": {
    "tags": [
      "scope:contract",
      "type:lib",
      "domain:shared"
    ]
  },
  "description": "Memory wiki page model \u2014 parse/apply/serialize, pure (no I/O)"
}
