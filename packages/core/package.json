{
  "name": "@rivetos/core",
  "version": "0.1.0",
  "description": "RivetOS agent runtime — the loop, the router, the lifecycle",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "echo 'core: tests pending'"
  },
  "dependencies": {
    "@rivetos/types": "*"
  }
}
