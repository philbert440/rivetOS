{
  "name": "@rivetos/types",
  "version": "0.1.0",
  "description": "Shared interfaces for RivetOS \u2014 Provider, Channel, Tool, Memory",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  }
}
