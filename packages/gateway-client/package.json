{
  "name": "@rivetos/gateway-client",
  "version": "0.4.0-beta.6",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/ package.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rivetos/types": "*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^4.1.5",
    "ws": "^8.18.0"
  },
  "nx": {
    "tags": [
      "scope:contract",
      "type:lib",
      "domain:shared"
    ]
  },
  "description": "Typed HTTP+WS client for the RivetOS gateway API — the only bridge RivetHub clients use"
}
