# @rivetos/mcp

SDK-agnostic MCP core for RivetOS — `ToolRegistration` and adapters from
RivetOS `Tool` objects onto the MCP wire.

The transport mount lives in `@rivetos/mcp-v2`.

```bash
npm install @rivetos/mcp
```

## License

Apache-2.0
