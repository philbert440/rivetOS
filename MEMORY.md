# MEMORY.md — Context Index

Lightweight index into the memory system. Run the referenced queries to pull context on demand.

## ⚠️ Critical Context
- **RivetOS v0.4.0** — First public beta. Node.js 24. All milestones M0-M8 complete.
- Repo: `github.com/philbert440/rivetOS` | Local: `/opt/rivetos`
- Docs site: rivetos.dev (Starlight)

## Project Status

| Milestone | Status | Summary |
|-----------|--------|---------|
| M0-M3 | ✅ Complete | Core runtime, providers, channels, tools |
| M4 | ✅ Complete | Learning & self-improvement (memory, skills, context loading) |
| M5 | ✅ Complete | Agent collaboration (delegation, routing, mesh groundwork, /model) |
| M6 | ✅ Complete | Containerized distribution (Docker, IaC, init wizard, CLI) |
| M7 | ✅ Complete | Reliability & polish (errors, circuit breakers, logging, mesh, security) |
| M8 | ✅ Complete | Documentation & launch (docs site, getting-started, plugin/skill guides) |

## Active Projects

| Project | Query | Notes |
|---------|-------|-------|
| RivetOS | `memory_search("rivetos milestone status")` | v0.4.0 public beta. All milestones done. |
| emkit.dev | `memory_search("emkit dev status")` | EM Ops Kit SaaS. Awaiting Phil's QA. |
| tompkins.app | `memory_search("tompkins app status")` | Family app. Next.js 16, Prisma 7. |
| GERTY training | `memory_search("gerty training lora status")` | Identity LoRA on V100 GPU 1. |

## Recent Work (April 2026)

| What | Query |
|------|-------|
| Provisioning & deployment | `memory_search("provision rsync NFS mesh")` |
| CI/CD fixes | `memory_search("CI workflows dockerfile node 24")` |
| Security hardening | `memory_search("secrets scan private IPs security")` |
| Telegram bug fix | `memory_search("telegram html tags double conversion")` |

## Architecture & Design

| Topic | Query |
|-------|-------|
| Multi-agent mesh | `memory_search("mesh registry delegation discovery")` |
| Plugin system | `memory_search("plugin discovery convention-based")` |
| Learning loop design | `memory_search("hermes learning loop design")` |
| Agent orchestration | `memory_search("delegation routing agent-driven")` |
| Hook system | `memory_search("hook pipeline lifecycle events")` |
| Skill system | `memory_search("skill creation autonomous")` |
| Container architecture | `memory_search("dockerfile docker compose datahub")` |

## Key Decisions

| Decision | Query |
|----------|-------|
| Node.js 24 upgrade | `memory_search("node 24 version bump")` |
| Plugin convention-based discovery | `memory_search("plugin registration standardize")` |
| Hermes-inspired learning | `memory_search("hermes learning loop LLM intelligence")` |
| Agent-driven routing | `memory_search("provider retry fallback error")` |
| NFS shared mount | `memory_search("shared mount NFS SMB cross-host")` |
| File size thresholds | `memory_search("file size split threshold 420 500 600")` |

## Lessons Learned

| Lesson | Query |
|--------|-------|
| Build artifact issues | `memory_search("build artifacts src gitignore")` |
| GERTY known-good config | `memory_search("gerty known good configuration")` |
| Config safety | `memory_search("config changes test first")` |
| Docker .dockerignore | `memory_search("dockerignore overly broad md exclude")` |
