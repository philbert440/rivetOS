{
  "name": "@rivetos/compaction-worker",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Compaction worker — graphile-worker tasks for bottom-up summarization (leaf/branch/root) and tool-call content synthesis",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rivetos/memory-postgres": "0.4.0-beta.6",
    "graphile-worker": "^0.16.6",
    "pg": "^8.20.0",
    "undici": "^8.1.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "nx": {
    "tags": ["scope:app", "type:app"]
  }
}
