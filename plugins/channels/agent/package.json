{
  "name": "@rivetos/channel-agent",
  "version": "0.1.0",
  "description": "Agent-to-agent channel — HTTP endpoint for cross-instance messaging",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@rivetos/types": "*"
  },
  "devDependencies": {
    "vitest": "^3.2.1"
  }
}
