{
  "name": "@rivetos/tool-search",
  "version": "0.1.0",
  "description": "File search tools (glob and grep) for RivetOS agents",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@rivetos/types": "*"
  }
}
