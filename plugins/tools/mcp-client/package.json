{
  "name": "@rivetos/tool-mcp-client",
  "version": "0.1.0",
  "description": "MCP client plugin — connects to MCP servers and registers their tools as native RivetOS tools",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@rivetos/types": "*"
  }
}
