{
  "name": "@rivetos/provider-openai-compat",
  "version": "0.1.0",
  "description": "OpenAI-compatible provider \u2014 llama-server, vLLM, LM Studio, OpenRouter, etc.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@rivetos/types": "*"
  }
}
