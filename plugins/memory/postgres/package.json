{
  "name": "@rivetos/memory-postgres",
  "version": "0.2.0",
  "description": "RivetOS Memory System — hybrid-scored search, background compaction, summary DAG",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "echo \"no tests yet\"",
    "migrate": "npx tsx src/migrate.ts"
  },
  "dependencies": {
    "@rivetos/types": "*",
    "pg": "^8.20.0"
  }
}
