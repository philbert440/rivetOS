{
  "name": "@rivetos/memory-postgres",
  "version": "0.1.0",
  "description": "Memory plugin — adapter over existing LCM PostgreSQL tables",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo 'tests pending'"
  },
  "dependencies": {
    "@rivetos/types": "*",
    "pg": "^8.13.0"
  }
}
