# /memory-today

Recall everything relevant from today (local time).

**Usage:**
```
/memory-today
/memory-today the frigate issue
```

Automatically uses `memory_browse(window="today")` (or equivalent) as the starting point, then applies topic filtering if provided.
