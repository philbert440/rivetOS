# /memory-yesterday

Recall everything relevant from yesterday (local time).

**Usage:**
```
/memory-yesterday
/memory-yesterday the DHCP problem
```

Starts with `memory_browse(window="yesterday")`.
