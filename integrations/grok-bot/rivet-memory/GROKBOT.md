# Grok Bot memory reflex

Query with memory_browse or memory_search. Write with memory_append or memory_ingest_session. Pass persona on writes when relevant; agent defaults to rivet-grokbot from the launcher env and should stay that way. Leave source unset so the launcher stamps grokbot. Do not use the Grok Build launcher.
