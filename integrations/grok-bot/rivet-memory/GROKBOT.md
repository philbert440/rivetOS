# Grok Bot memory reflex

Query with memory_browse or memory_search. Write with memory_append or memory_ingest_session. Pass role (user, assistant, system, or tool), agent, and persona on writes. Leave source unset so the launcher stamps grokbot. Do not use the Grok Build launcher.
