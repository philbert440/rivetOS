{
  "name": "@rivetos/opencode-rivet-memory-capture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capture worker for the OpenCode CLI rivet-memory integration. Ingests OpenCode SQLite sessions (--ingest-session / --backfill) into the shared RivetOS memory store under agent='rivet-glm'.",
  "license": "Apache-2.0",
  "main": "dist/opencode-memory-capture.js",
  "bin": {
    "opencode-rivet-memory-capture": "dist/opencode-memory-capture.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p ../plugin/tsconfig.json",
    "test": "tsx test/smoke.test.ts && vitest run",
    "clean": "rm -rf dist",
    "lint": "eslint src/ package.json"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/pg": "^8.20.3"
  }
}
